#include "main.h"
/**
 * swap_int - resetting
 * @a : parameter
 * @b : parameter
 * Description: Describing the function
 */
void swap_int(int *a, int *b)
{
int c = (*a);
(*a) = (*b);
(*b) = c;
}
