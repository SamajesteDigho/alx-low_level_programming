#include "main.h"
/**
 * _strlen - string length
 * @s : parameter
 * Description: Describing the function
 * Return: Return number of caracters
 */
int _strlen(char *s)
{
return (strlen(s));
}
